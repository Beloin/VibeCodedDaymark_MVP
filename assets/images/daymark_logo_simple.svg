<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512" role="img" aria-label="Colorful D icon">
  <!-- Simplified version without filter for better Flutter compatibility -->
  
  <!-- left rounded bar (the vertical stroke of the D) -->
  <rect x="64" y="116" width="120" height="280" rx="60" ry="60" fill="#34A853"/>

  <!-- curved right part built from three colored arcs (stroke only) -->
  <!-- arc geometry: center (256,256), radius 180 -->
  <!-- top arc - blue -->
  <path d="M256 76 A180 180 0 0 1 411.885 166"
        fill="none" stroke="#4285F4" stroke-width="100" stroke-linecap="round" />

  <!-- middle arc - red -->
  <path d="M411.885 166 A180 180 0 0 1 411.885 346"
        fill="none" stroke="#EA4335" stroke-width="100" stroke-linecap="round" />

  <!-- bottom arc - yellow -->
  <path d="M411.885 346 A180 180 0 0 1 256 436"
        fill="none" stroke="#FBBC05" stroke-width="100" stroke-linecap="round" />

  <!-- small inner white cut so the inside of the 'D' reads as negative space -->
  <rect x="120" y="156" width="120" height="200" rx="50" ry="50" fill="white"/>
</svg>